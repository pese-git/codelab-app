## 1. State и видимость палитры

- [x] 1.1 Добавить `isCommandPaletteOpen` (и, если нужно, список доступности команд) в `CodeLabShellState`
- [x] 1.2 Реализовать `openCommandPalette()`/`closeCommandPalette()` в `CodeLabShellCubit` вместо заглушки `_recordPendingAction(...)`
- [x] 1.3 Смонтировать `AcpCommandPaletteSurface` как overlay поверх `AcpWorkbenchLayout` в `workbench_shell.dart`, управляемый `state.isCommandPaletteOpen`
- [x] 1.4 Убедиться, что `Esc` при открытой палитре закрывает именно её, а не триггерит `onCancel` шелла (проверить взаимодействие с `AcpWorkbenchShortcuts`)

## 2. Выполнение доступных команд

- [x] 2.1 `/new` → вызывает существующий `createSession()`, закрывает палитру
- [x] 2.2 `/reconnect` → вызывает существующий `reconnect()`, закрывает палитру
- [x] 2.3 `/logs` → делает видимым `AcpDebugLogPanel` (снимает `Offstage` с инспектора в narrow-layout, если он был скрыт), закрывает палитру

## 3. Честное отображение недоступных команд

- [x] 3.1 Пометить `/plan`, `/permissions`, `/compact` как недоступные в `AcpCommandAction`/палитре (disabled-стиль или "coming soon"), без изменений в `acp_ui`, если атрибут доступности уже поддержан, либо с точечным расширением `AcpCommandAction`, если нет
- [x] 3.2 Убедиться, что выбор недоступной команды не закрывает палитру и не пишет в диагностику сообщение, похожее на выполненное действие

## 4. Тесты

- [x] 4.1 Widget-тест: `Ctrl/Cmd+K` открывает палитру, `Esc` закрывает
- [x] 4.2 Widget-тест: выбор `/new` создаёт сессию и закрывает палитру
- [x] 4.3 Widget-тест: выбор `/reconnect` вызывает reconnect-флоу и закрывает палитру
- [x] 4.4 Widget-тест: выбор `/logs` показывает debug log panel (проверка снятия `Offstage` в narrow-layout вынесена в `acp_ui` — полный `CodeLabApp` на узкой ширине упирается в отдельный, несвязанный overflow командной панели)
- [x] 4.5 Widget-тест: выбор `/plan`/`/permissions`/`/compact` оставляет палитру открытой и не создаёт ложной записи в диагностике

## 5. Inline-триггер `/` в композере

- [x] 5.1 Добавить `AcpCommandPaletteSurface.showSearchField`/`queryOverride` — inline-режим без собственного `TextBox`, фильтрация по внешнему значению
- [x] 5.2 Реализовать детектирование "`/` — первый символ нового слова" в `AcpPromptComposer`/презентационном слое (позиция 0 или сразу после пробела/переноса строки)
- [x] 5.3 Смонтировать inline-палитру как плавающий слой над композером при срабатывании триггера (реализовано через `OverlayPortal`+`CompositedTransformFollower`/`Target` в `AcpPromptComposer` — не буквальный `Positioned` внутри локального `Stack`, но по факту именно floating-слой над композером, не подчинённый layout-границам его родителей)
- [x] 5.4 Реализовать `↑`/`↓` навигацию по списку и `Enter`-выбор (переопределяя вставку переноса строки только в этом состоянии)
- [x] 5.5 Реализовать закрытие по `Esc` (текст остаётся) и по удалению триггерного `/` (backspace)
- [x] 5.6 Реализовать очистку `/word` из текста композера и выполнение команды при выборе — тот же обработчик, что и для `Ctrl/Cmd+K`-палитры

## 6. Тесты — inline-триггер

- [x] 6.1 Widget-тест: `/` в начале композера открывает inline-палитру (`acp_ui/test/acp_molecules_test.dart`)
- [x] 6.2 Widget-тест: `/` сразу после непробельного символа не открывает палитру
- [x] 6.3 Widget-тест: продолжение набора текста фильтрует список
- [x] 6.4 Widget-тест: `Enter` при открытой inline-палитре выбирает подсвеченную команду, не вставляет перенос строки
- [x] 6.5 Widget-тест: вне inline-режима `Enter` не перехватывается inline-обработчиком (эмпирически проверено: baseline-поведение этого `TextBox` для одиночного `Enter` — не вставлять перенос строки и не сабмитить; тест фиксирует именно "как раньше", а не буквально "вставляет \n" — см. design.md-допущение, которое не подтвердилось при проверке)
- [x] 6.6 Widget-тест: выбор команды очищает `/word` из композера
- [x] 6.7 Widget-тест: удаление триггерного `/` закрывает inline-палитру без выполнения команды

## 7. Агентские команды (`AvailableCommandsUpdate`)

- [x] 7.1 Добавить `source`/`isFromAgent` в `AcpCommandAction` (`acp_ui`) для визуального разделения секций "client-native"/"From agent"
- [x] 7.2 Добавить `agentCommands: List<AcpCommandAction>` (или аналог) в `CodeLabShellState`, маппить из `SessionUpdate.availableCommandsUpdate` вместо (или в дополнение к) текущей debug-записи в инспекторе
- [x] 7.3 Заменять `agentCommands` целиком на каждый новый `availableCommandsUpdate`; очищать при смене активной сессии/потере соединения
- [x] 7.4 Палитра (`Ctrl/Cmd+K` и inline) рендерит объединённый список: шесть client-native + `state.agentCommands`, разделённые визуально, единая фильтрация через `AcpCommandAction.filter()`
- [x] 7.5 Выбор агентской команды вставляет `/{name} ` (+ `input.hint`, если есть, показывается серым suffix-placeholder'ом в `TextBox` — исчезает, как только пользователь печатает что-то после `/{name} `) в композер вместо прямого вызова метода кубита; палитра закрывается, фокус остаётся в композере

## 8. Тесты — агентские команды

- [x] 8.1 Widget-тест: `availableCommandsUpdate` с 1+ командами добавляет их в палитру отдельной секцией
- [x] 8.2 Widget-тест: повторный `availableCommandsUpdate` заменяет предыдущий список агентских команд целиком
- [x] 8.3 Widget-тест: без единого `availableCommandsUpdate` палитра показывает только шесть client-native команд
- [x] 8.4 Widget-тест: выбор агентской команды вставляет `/{name} ` в композер и закрывает палитру, не выполняя её как локальное действие
- [x] 8.5 Widget-тест: смена активной сессии очищает список агентских команд предыдущей сессии

## 9. Проверка

- [x] 9.1 `fvm dart run melos run format`
- [x] 9.2 `fvm dart run melos run analyze`
- [x] 9.3 `fvm dart run melos run test`
