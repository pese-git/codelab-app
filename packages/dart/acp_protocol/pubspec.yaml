name: acp_protocol
description: Typed ACP JSON-RPC protocol models, codecs, and validation.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ">=3.8.0 <4.0.0"

dependencies:
  fpdart: ^1.2.0
  freezed_annotation: ^3.1.0
  json_annotation: ^4.9.0
  meta: ^1.16.0

dev_dependencies:
  build_runner: ^2.10.5
  freezed: ^3.2.5
  json_serializable: ^6.11.3
  lints: ^6.0.0
  test: ^1.26.3
